version: '3.8' 
 
services: 
  sarcasm-detector: 
    build: . 
    ports: 
      - "8000:8000" 
    volumes: 
      - ./models:/app/models 
    environment: 
      - ENVIRONMENT=production 
    restart: unless-stopped 
